# 开发工作流程

## 项目设置

1. 确保使用 Python 3.12+
2. 使用 uv 管理依赖：

   ```bash
   uv install
   uv run python -m src.app.server
   ```

## 开发流程

1. **新功能开发**
   - 从 `main` 分支创建功能分支
   - 遵循命名约定：`feature/功能名称`
   - 实现功能并添加测试

2. **代理开发**
   - 在 `src/agent/sub_agents/` 下创建新目录
   - 实现 `agent.py`、`prompt.py` 和 `tools/`
   - 在主代理中注册新的子代理

3. **API 开发**
   - 在 `src/app/` 下添加新的路由
   - 使用 Pydantic 模型进行数据验证
   - 添加适当的错误处理

## 代码质量

- 使用类型注解
- 编写文档字符串（中文）
- 遵循 PEP 8 标准
- 添加单元测试

## 依赖管理

- 使用 [pyproject.toml](mdc:pyproject.toml) 管理项目依赖
- 新依赖添加后运行 `uv lock`
- 定期更新依赖版本

## 环境配置

- 使用 `.env` 文件存储敏感配置
- 在 [config.py](mdc:src/app/config.py) 中定义配置类
- 不要将 `.env` 文件提交到版本控制

## 调试和日志

- 使用 Python 标准库的 `logging` 模块
- 为不同组件设置不同的日志级别
- 在生产环境中关闭调试模式
