# 测试开发指南

## 测试结构

推荐的测试目录结构：

```text
tests/
├── unit/
│   ├── test_agents/
│   │   ├── test_anki_agent.py
│   │   ├── test_summarize_agent.py
│   │   └── test_translate_agent.py
│   └── test_app/
│       ├── test_server.py
│       └── test_ocr.py
├── integration/
└── conftest.py
```

## 测试命名约定

- 测试文件：`test_*.py`
- 测试类：`Test*`
- 测试方法：`test_*`

## 代理测试

```python
import pytest
from src.agent.sub_agents.anki_agent.agent import AnkiAgent

class TestAnkiAgent:
    def setup_method(self):
        """每个测试方法前的设置"""
        self.agent = AnkiAgent(config={})
    
    def test_create_anki_card(self):
        """测试 Anki 卡片生成"""
        content = "测试内容"
        result = self.agent.create_card(content)
        
        assert result is not None
        assert "question" in result
        assert "answer" in result
    
    @pytest.mark.asyncio
    async def test_async_processing(self):
        """测试异步处理"""
        # 异步测试逻辑
        pass
```

## API 测试

```python
from fastapi.testclient import TestClient
from src.app.server import app

client = TestClient(app)

def test_process_endpoint():
    """测试处理端点"""
    response = client.post("/api/v1/process", json={
        "content": "测试内容",
        "agent_type": "anki"
    })
    
    assert response.status_code == 200
    assert response.json()["status"] == "success"
```

## 测试工具

- 使用 pytest 作为测试框架
- 使用 pytest-asyncio 进行异步测试
- 使用 TestClient 进行 API 测试
- 使用 mock 进行外部依赖模拟

## 测试覆盖率

- 目标覆盖率：80%+
- 重点测试核心业务逻辑
- 测试错误处理分支
