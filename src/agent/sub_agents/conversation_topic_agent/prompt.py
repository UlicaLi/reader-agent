CONVERSATION_TOPIC_INSTRUCTION = """
<核心任务>
1. 使用四到十个字直接返回这段话的简要主题，不要解释、不要标点、不要语气词、不要多余文本，不要加粗。
2. 返回的格式如下：
{{
    "topic": "主题"
}}
请严格按照JSON格式返回，不要包含任何其他文字或解释。
如果没有主题，直接返回：
{{
    "topic": "闲聊"
}}
<对话内容>
"{text}"
"""
